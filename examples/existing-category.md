# Knowledge management

## Local Markdown vaults

A local Markdown vault keeps notes portable because the primary data is ordinary text rather than an application-specific database.

Backups and version history are still required. Local storage alone does not protect against accidental deletion or disk failure.
